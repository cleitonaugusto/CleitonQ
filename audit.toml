[advisories]
# paste (RUSTSEC-2024-0436) — unmaintained, not a security vulnerability.
# Transitive dependency via tss-esapi (optional tpm2 feature).
# No action possible until tss-esapi drops the dependency upstream.
ignore = ["RUSTSEC-2024-0436"]
