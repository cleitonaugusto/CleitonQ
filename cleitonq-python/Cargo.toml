[package]
name = "cleitonq-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "cleitonq"
crate-type = ["cdylib"]

[dependencies]
cleitonq = { path = ".." }
pyo3 = { version = "0.22", features = ["extension-module"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
